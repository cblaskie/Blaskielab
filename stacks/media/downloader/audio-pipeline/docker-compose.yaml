version: "3.8"

services:
  # 1. Spotify Downloader (Alpine with Root Workspaces Fixed)
  spotdl:
    image: spotdl/spotify-downloader:latest
    container_name: spotdl
    restart: unless-stopped
    entrypoint: >
      sh -c "
      apk add --no-cache gcompat libc6-compat &&
      ln -sf /lib/libc.musl-x86_64.so.1 /usr/lib/libresolv.so.2 &&
      exec spotdl web --host 0.0.0.0 --web-use-output-dir
      "
    volumes:
      # Maps your music folder directly to spotDL's internal output path
      - /mnt/user/world/personal/shared/yoto:/music
      # FIXED: Overrides spotDL's active logging configuration directory paths
      - /mnt/user/appdata/spotdl:/config
    environment:
      - TZ=${TZ}
      # FIXED: Forces the application to route all configuration and log files 
      # away from root system folders into your persistent appdata volume
      - SPOTDL_HOME=/config
    working_dir: /music
    networks:
      - proxy_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.spotdl.rule=Host(`spotdl.theblaskies.com`)"
      - "traefik.http.routers.spotdl.entrypoints=websecure"
      - "traefik.http.routers.spotdl.tls.certresolver=cloudflare"
      - "traefik.http.services.spotdl.loadbalancer.server.port=8800"

  # 2. YouTube-to-Audio (Metube)
  youtube-dl:
    image: alexta69/metube:latest
    container_name: youtube_audio_dl
    user: "99:100"
    restart: unless-stopped
    volumes:
      - /mnt/user/world/personal/shared/yoto:/downloads
    environment:
      - OUTPUT_TEMPLATE=%(title)s.%(ext)s
      - YTDL_OPTIONS={"extract_audio":true,"audio_format":"mp3","quality":"0"}
    networks:
      - proxy_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ytdl.rule=Host(`ytdl.theblaskies.com`)"
      - "traefik.http.routers.ytdl.entrypoints=websecure"
      - "traefik.http.routers.ytdl.tls.certresolver=cloudflare"
      - "traefik.http.services.ytdl.loadbalancer.server.port=8081"

networks:
  proxy_net:
    external: true
