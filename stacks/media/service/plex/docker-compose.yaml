services:
  plex:
    image: ghcr.io/linuxserver/plex:latest@sha256:1720efa8e919a724ff3003cce7c1c0ae91a54e097ca3c8f6713a780c6fd73432
    container_name: plex
    restart: unless-stopped
    runtime: nvidia

    env_file:
      - .env

    volumes:
      - /mnt/user/appdata/plex:/config
      - /mnt/user/data/media:/media
    tmpfs:
      - /transcode:size=16G

    networks:
      - media_net
      - proxy_net

    labels:
      - traefik.enable=true
      - traefik.http.routers.plex.rule=Host(`plex.theblaskies.com`)
      - traefik.http.routers.plex.entrypoints=websecure
      - traefik.http.routers.plex.tls.certresolver=cloudflare
      - traefik.http.services.plex.loadbalancer.server.port=32400

networks:
  media_net:
    external: true
  proxy_net:
    external: true
