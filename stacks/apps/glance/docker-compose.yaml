version: "3.8"

services:
  # 3. Central System Dashboard & Cockpit
  glance:
    image: glanceapp/glance:latest
    container_name: glance
    restart: unless-stopped
    
    # 🎯 THE FIX: Explicitly tell Glance exactly where to read the config file inside the mount
    environment:
      - GLANCE_CONFIG=/app/config/glance.yml
      
    volumes:
      # Mounts the folder containing your docker-compose and glance.yml directly
      - /etc/komodo/repos/Blaskielab/stacks/apps/glance:/app/config:ro
    networks:
      - proxy_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.glance.rule=Host(`home.theblaskies.com`)"
      - "traefik.http.routers.glance.entrypoints=websecure"
      - "traefik.http.routers.glance.tls.certresolver=cloudflare"
      
      # 🔐 SECURED: Protected by PocketID authentication gate and error handlers
      - "traefik.http.routers.glance.middlewares=pocketid-auth@file,pretty-errors@file"
      
      # Target the standard internal port Glance natively uses
      - "traefik.http.services.glance.loadbalancer.server.port=8080"

networks:
  proxy_net:
    external: true
