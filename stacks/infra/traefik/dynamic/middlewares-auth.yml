http:
  services:
    # Maps your error-pages container to the file provider
    error-pages-service:
      loadBalancer:
        servers:
          - url: "http://error-pages:8080"

  middlewares:
    # ⬇️ ADDED: The new simplified Traefik Forward Auth middleware
    pocketid-auth:
      forwardAuth:
        address: "http://traefik-forward-auth:4181"
        trustForwardHeader: true
        authResponseHeaders:
          - "X-Forwarded-User"

    # ⬇️ KEPT: Your Shuffle-themed Error Dashboard Middleware
    pretty-errors:
      errors:
        status:
          - "404"
          - "500-504" # Intercepts app crashes, bad gateways, and connection timeouts
        service: error-pages-service
        query: "/{status}.html"
